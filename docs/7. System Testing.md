# System Testing

| 시나리오 | 액션 | 예상 결과 | S/F |
| --- | --- | --- | --- |
| 시스템 부팅 및 모니터링 모드 | 1. 보드에 전원 인가함 <BR>2. 버튼1을 3번 누름 | 1-1. 전원 LED(1번) 켜짐 <BR>1-2. RGB LED 녹색으로 켜짐 <BR>1-3. FND와 현재 온/습도/밝기 표시됨 <BR>1-4. OLED에 모니터링 모드 표시됨 <BR>2-1. 모니터링 → 수동 제어 → 보안 → 모니터링 순환 | S |
| 자동 조명 제어 | 모니터링 모드에서 CDS를 완전히 가렸다가 천천히 뗌 | 1) 가리면 메인 조명(LED 8) 최대 밝기됨 <BR>2) 손을 떼면 주변 밝기에 따라 조명이 서서히 어두워짐 | S |
| 자동 팬 제어 | 모니터링 모드에서 드라이기 등으로 DHT11 온도를 28℃ 이상으로 올림 | 1) 온도 28℃ 초과 시 DC 모터(팬) 회전 시작함 <BR>2) 온도 하강 시 팬 정지함 | S |
| 수동 모드로 전환 | 버튼 1을 한 번 누름 | 1) RGB LED 파란색으로 변경됨 <BR>2) OLED 1번째 줄에 “Mode: Maunual”, 2번째 줄에 **"Selected: DoorLock"** 표시됨 | S |
| 수동 장치 제어 1 (서보) | 수동 모드에서 **버튼 3(열림)** 누르고 잠시 후 **버튼 4(닫힘)** 누름 | 1) 버튼 3: 서보 모터가 ‘열림’ 위치로 회전함 <BR>2) 버튼 4: 서보 모터가 ‘닫힘’ 위치로 복귀함 | S |
| 수동 장치 제어 2 (스텝) | 버튼 2를 눌러 제어 대상을 블라인드로 변경 → **버튼 3(정회전)**, **버튼 4(역회전)** 각각 누름 | 1) 버튼 2: OLED에 **"Selected: Blinds"** 표시됨 <BR>2) 버튼 3: 스텝 모터가 설정 스텝만큼 정방향 회전함 <BR>3) 버튼 4: 스텝 모터가 역방향 회전함 | S |
| 수동 장치 제어 3 (릴레이) | 버튼 2를 눌러 제어 대상을 릴레이로 변경 → **버튼 3(On)**, **버튼 4(Off)** 각각 누름 | 1) 버튼 2: OLED에 “Selected: Relay” 표시됨 <BR>2) 버튼 3: 릴레이가 On <BR>4) 버튼 4: 릴레리가 Off | S |
| 메인 조명 수동 제어 | 수동 제어 모드에서 VR을 끝까지 돌렸다가 다시 첨으로 되돌림 | VR의 돌림 정도의 따라 메인 조명(LED 8)이 비례해서 밝아졌다 어두워짐 | S |
| 보안 모드로 전환 | 버튼 1을 한 번 더 누름 | 1) RGB LED 빨간색으로 변경됨 <BR>2) 보안 대기 LED(2번) 점등됨 <BR>3) OLED에 **"Mode: Security"** 표시됨 | S |
| 보안 이벤트 발생 | 보안 모드에서 초음파 센서 앞 30cm 이내에 손을 가져다 댐 | 1) 즉시 부저 2초간 울림 <BR>2) 경고 LED(4,5,6번) 고속 점멸 시작함 <BR>3) 보안 대기 LED(2번) off <BR>4) 버튼 1을 눌러도 모드 변경되지 않음 | S |
| 상태 수신 (CAN) | PC의 CANView에서 현재 시스템의 상태를 확인 | 현재 모드, 보안 경고 상태, 온습도/밝기 정보 표시 | S |
| 원격 모드 변경 | PC의 CANView에서 모드 변경 명령 보냄 (ID: 20, DATA: 01, 02, 00) | 모니터링 모드 → 매뉴얼 모드 → 보안 모드 → 모니터링 모드 순환  | S |
| 원격 장치 제어 | PC의 CANView에서 장치 변경 및 제어 명령 보냄 | 1) 도어락(서보) <BR>2) 블라인드(스텝) <BR>3) 외부전원(릴레이) | S |
| 원격 경고 해제 (CAN) | PC의 CAN 분석 툴에서 ID `0x21`(CAN_ID_CMD_ALARM_OFF) 메시지를 보드로 전송함 | 1) 경고 LED 점멸이 즉시 멈추고 꺼짐 <BR>2) 시스템이 보안 대기 상태로 복귀하여 보안 대기 LED(2번)이 다시 점등됨 | S |

- **전체 태스크 통합 시 OLED 초기화 직후 시스템 멈춤**
    
    → 모든 태스크+RTOS 객체 통합 시 Heap 부족
    
    → 기록: *"configTOTAL_HEAP_SIZE 증설(20KB → 24KB)로 해결"*
    
- **태스크 스택 분배 및 충돌 문제**
    
    → 각 태스크별 스택 사용량 점검
    
    → 기록: *"스택 부족 시 HardFault → 태스크별 여유 있게 설정, 모니터링으로 확인"*
    
    <img width="733" height="532" alt="image" src="https://github.com/user-attachments/assets/4c1e8f57-3f1e-424e-b2ca-7fbbe051b80e" />

    
- **우선순위/인터럽트 설계 원칙 정립**
    
    → NVIC와 FreeRTOS 간 역할 분리, 우선순위 조정
    
    → 기록: *"긴급 이벤트 = NVIC, 로직 처리 = Task, FreeRTOS 설정(configASSERT 등) 반영"*
