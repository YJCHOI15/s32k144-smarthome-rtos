# Unit Testing

각 모듈/함수가 **설계된 대로 단일 기능을 정확히 수행하는지** 검증한다.

| 파일명 | 주요 함수 | 테스트 항목 | 동작 | S/F |
| --- | --- | --- | --- | --- |
| `shh_led.c` | `SHH_LEDs_Init()` | 전원 LED만 ON, 나머지 OFF 확인 | LED1=ON, RED/GREEN/BLUE/LED2/4/5/6=OFF | S |
|  | `SHH_ModeLED_Set()` | 모드에 따른 RGB LED 점등 | GREEN / BLUE / RED 각각 ON | S |
|  | `SHH_SecurityStandbyLED_On/Off()` | Standby LED 제어 | LED2 ON / OFF | S |
|  | `SHH_SecurityWarningLED_On/Off()` | Warning LED 제어 | LED4/5/6 ON / OFF | S |
|  | `SHH_SecurityWarningLED_Toggle()` | Warning LED 토글 | LED4/5/6 상태 반전 | S |
|  | `SHH_MainLight_SetBrightness()` | 메인 조명 PWM 제어 | LED8 밝기 0%, 중간, 최대 | S |
| `shh_actuator.c` | `SHH_Fan_On/Off()` | 팬 구동 제어 | DC 모터 핀 HIGH/LOW | S |
|  | `SHH_DoorLock_Open/Close()` | 서보 모터 위치 제어 | Servo 90° / 0° 이동 | S |
|  | `SHH_Blinds_Move()` | 스텝 모터 이동 | 블라인드 전/역방향 회전 | S |
|  | `SHH_ExternalPower_On/Off()` | 릴레이 구동 | 외부 전원 공급/차단 | S |
| `shh_display.c` | `SHH_FND_Init()` | 모든 자릿수 초기화 | FND 셀 모두 OFF | S |
|  | `SHH_FND_BufferUpdate()` | 버퍼 값 갱신 | 버퍼에 올바른 자리 저장 | S |
|  | `SHH_FND_Display()` | 멀티플렉싱 표시 | 각 자리 숫자가 순차 점등 | S |
|  | `SHH_OLED_Init()` | OLED 초기화 | 화면 ON, 반전 설정 적용 | S |
|  | `SHH_OLED_Clear()` | 화면 클리어 | 모든 픽셀 OFF | S |
|  | `SHH_OLED_SetCursor()` | 커서 위치 이동 | 지정 좌표로 쓰기 시작 | S |
|  | `SHH_OLED_PrintChar()` | 문자 출력 | OLED에 A 표시 | S |
|  | `SHH_OLED_PrintString_5x8()` | 문자열 출력 | OLED에 HOME 표시 | S |
| `shh_sensor.c` | `SHH_ReadTemperature()` | 온도 읽기 | 온도 반환 / 마지막 값 유지 | S |
|  | `SHH_ReadHumidity()` | 습도 읽기 | 습도 값 반환 | S |
|  | `_SHH_Read_DHT11_40bit_Data()` | 40bit 데이터 수신 | 데이터+체크섬 일치 시 true | S |
|  | `SHH_ReadBrightnessSensor()` | 조도 센서 변환 | 0% / 100% 반환 | S |
|  | `SHH_ReadManualControlVr()` | VR 입력 변환 | 약 50% 반환 | S |
|  | `SHH_uWave_StartMeasurement()` | 초음파 트리거 출력 | TRIG 10µs HIGH | S |
|  | `SHH_uWave_GetDistanceCm()` | 거리 계산 | 약 10cm 반환 | S |
| `shh_sound.c` | `SHH_Buzzer_StartAlarm/StopAlarm()` | 알람 부저 제어 | BUZZER 핀 HIGH/LOW | S |
|  | `SHH_Piezo_Beep()` | 피에조 단발음 출력 | 짧은 소리 발생 | S |
| `shh_system.c` | `SHH_Init()` | 시스템 초기화 | 모든 핀/드라이버/인터럽트 정상 설정 | S |
| `shh_uart.c` | `SHH_Uart_Printf()` | 포맷 문자열 UART 출력 | UART에 `"Temp=25"` 출력, 태스크 동시 호출 시도해도 로그 깨짐 없음 | S |

- **온습도 센서 타이밍 문제 (µs 단위 딜레이 불일치)**
    
    → `_SHH_Read_DHT11_40bit_Data()` 단위 함수 검증 과정
    
    → 기록: *"MCU 딜레이 함수 오차로 센서값 0 출력 → µs 구간 비교 로직 수정으로 해결"*
    
    <img width="1419" height="200" alt="image" src="https://github.com/user-attachments/assets/3cf399d4-13dc-4802-ada5-a8eeeab95a8a" />
    
    <img width="801" height="313" alt="image" src="https://github.com/user-attachments/assets/be8b0f00-5125-45b8-9ab9-0158df5a2b4d" />
    
    <img width="680" height="177" alt="image" src="https://github.com/user-attachments/assets/933f7db6-f3f8-45b2-a576-423c465a06ef" />
    
- **PWM 자원 충돌 문제 (LED 8, Servo)**
    
    → LED는 high true pulses, 서보는 low true pulses가 필요
    
    → 두 장치의 사용 모듈을 각각 FTM0(서보), FTM1로 분리함
